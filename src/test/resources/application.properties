# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=36000000

google.client.id=${GOOGLE_CLIENT_ID}
google.client.secret=${GOOGLE_CLIENT_SECRET}

# Supabase PostgreSQL Configuration
# Corrected Single URL Format
spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?user=postgres.vrphuqiramsedmejyrxe&password=3eELDIdGD9O7wV0I&prepareThreshold=0

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# **CRITICAL FIX for Dialect Detection**
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.cookie.secure=false